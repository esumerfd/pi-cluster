[Unit]
Description=Hailo-10H Web Dashboard
After=network.target

[Service]
Type=simple
User={{ dashboard_user }}
WorkingDirectory={{ dashboard_dir }}
ExecStart=/usr/bin/python3 {{ dashboard_dir }}/server.py {{ dashboard_port }}
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
